#pragma once
#include "Optimization/interpolation.h"


/*
class STOFitter {
private:
    double          m_alpha = 1.0;
    double          m_pi    = -1;
    static int      m_n;

    double s1(double r);
    static void G(  const alglib::real_1d_array &c,
                    const alglib::real_1d_array &x,
                    double                      &func,
                    void                        *ptr);
    static void gradient(   const alglib::real_1d_array &c,
                            const alglib::real_1d_array &x,
                            double                      &func,
                            alglib::real_1d_array       &gradient,
                            void                        *ptr);

    static void hessian(    const alglib::real_1d_array &c,
                            const alglib::real_1d_array &x,
                            double                      &func,
                            alglib::real_1d_array       &gradient,
                            alglib::real_2d_array       &hess,
                            void                        *ptr);


public:
    STOFitter(double alpha);
    void computeFit(int n);
};
*/
